using System;

namespace Loop
{
    public class Loop
    {
        bool valueFound = false;        for (int i = 0; i < 100; i++)         {            Console.WriteLine(array[i]);            if (array[i] == expectedValue)             {                valueFound = true;   	            break;            }         }        if (valueFound)        {            Console.WriteLine("Value Found");        }        // More code here
        
    }
}
